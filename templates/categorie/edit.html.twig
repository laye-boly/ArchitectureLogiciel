{% extends 'base.html.twig' %}
{% block body %}
<div style="margin: 30px;">
 {{form_start(form)}}
    <div class="form-group">
 	{{ form_label(form.nom, "Le nom de la catégorie") }}
 	{{ form_widget(form.nom, {'attr': {'class': 'form-control'}}) }}
 </div> 
    
    {{form_row(form.submit)}}
 {{form_end(form)}}

 </div>
{% endblock %}

